# Repo-specific ignores only. The global git excludes live in
# git/.config/git/ignore (stowed to ~/.config/git/ignore).
.DS_Store
*.bak

# Zed writes its prompt-library LMDB into the stowed config dir at runtime.
zed/.config/zed/prompts/

# Private Claude config: cloned manually, see setup/claude-config.sh
claude-config-*/
.claude/
.superpowers/
